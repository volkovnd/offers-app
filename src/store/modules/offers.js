const state = () => ({});

/** @type {import("vuex").MutationTree} */
const mutations = {};

/** @type {import("vuex").ActionTree} */
const actions = {};

/** @type {import("vuex").GetterTree} */
const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
  namespaced: true,
};
